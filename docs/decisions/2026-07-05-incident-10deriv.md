# インシデント記録: 10_deriv 約1ヶ月無取引（2026-07-05）

> 1行サマリー: 障害ではなくbot自身の複合欠陥。シグナル3経路すべてが構造的に発火不能で target_position が0.2固定となり、デッドバンド（|delta|<0.05はスキップ）に吸収されて2026-06-06以降無取引。**対応は人間承認待ち**（§7行0エスカレーション凍結中）。

調査: シャドー運用P2-02のT3タスクとして Fable 5 / Opus 4.8 が独立に実施し、同一結論に到達（相互裏取り済み）。

## 確定した欠陥

| # | 欠陥 | 証拠 |
|---|------|------|
| A | `funding_extreme_pct=0.01`(1%) に対し Kraken Futures の実FRは全1,778レコードで最大 \|6.1e-5\| — 閾値が実値の約160倍で数学的に到達不能。docstringはBinanceだが実装はKraken＝取引所ミスマッチの名残 | `src/bots/bot_10_deriv.py:57,66` / `src/config.py:179` / derivatives全件集計 |
| B | `derivatives.open_interest` が全1,778行で0.0 → OI分岐も到達不能 | `src/data_collector.py:220` の `... or 0` フォールバックが握り潰している可能性。Actionsログ未確認 |
| C | 保存→取得の順序バグ: 今回分を保存した直後に「最新」を前回値として取得するため `oi_change` が恒等的に0（OIが直っても機能しない） | `src/bots/bot_10_deriv.py:45→48` / `src/database.py:374-384` |
| - | 付随: `cooldown_bars: 6` は実装から一切参照されない死にパラメータ | `src/config.py:181` |

結果、常に「デリバ中立→target 0.2」。6/6以降301回の実行でポジション比率のドリフト最大0.0305 < 0.05（発火閾値）のため無取引。**simulator・データ収集基盤は正常**。

## 対応案（すべて人間承認待ち）

1. **第一段（純バグ修正・セットで）**: (C)の順序修正 ＋ (B)のOI取得原因調査（まず Actions ログで `fetch_open_interest` の warning 有無を確認 — §6.5鉄則: ログ確認前の推測修正禁止）。
2. **第二段（§6.3チューニング）**: `funding_extreme_pct` 0.01 → 実FR分布に合う値（観測最大6e-5を踏まえ 2e-5〜5e-5 目安）。エスカレーション解除後に1bot・評価7日で。
3. `POSITION_CHANGE_THRESHOLD`（全bot共通値）の変更は非推奨・人間専権。

## 週次レビューへの訂正

`2026-07-05-weekly.md` の行「10_deriv 機能不全 → エントリー条件が厳しすぎないか点検」は、本調査により「条件が厳しい」のではなく「**シグナルが構造的に発火不能**」と確定。次回レビューは本記録を参照のこと。

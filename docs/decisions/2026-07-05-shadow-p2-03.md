# シャドー運用 P2-03: §6.1改訂の回帰検証（2026-07-05）

> 1行サマリー: P2-02で発見した誤警報（古いローカルクローンを根拠にインシデント判定）が、改訂版§6.1でOpus 4.8により**正しく「✅正常」判定に変わることを確認**。仕様改訂の有効性を実証し、P2/P3を完了とする。

## 検証内容

P2-02のT1と同一プロンプトで、改訂後の§6.1（手順0: `git fetch origin main` で本番鮮度確認、閾値6時間）に基づくデイリーヘルスチェックをOpus 4.8に再実行させた。

| 観点 | 改訂前（P2-02） | 改訂後（本検証） |
|---|---|---|
| 判定 | 🔴 インシデント（誤警報） | ✅ 正常（正解） |
| 鮮度の根拠 | 手元の古いクローンのDB | origin/main の最新bot実行コミット（39分前） |
| cron遅延の扱い | 停止と誤認 | 「6時間未満は記録のみ」と正しく分類 |
| 特筆 | — | `git show origin/main:data/trading_bot.db` で本番DBを抽出して分析（仕様の意図を超えた正しい工夫） |

## 軽微な指摘（アクションに影響なし）

- 停止5botの「初回停止日」を `MIN(timestamp) WHERE is_active=0`（史上初の停止）で集計しており、現在の停止エピソードの開始日（＝最後に is_active=1 だった時刻の直後。実際は2026-05-16〜06-04）とは異なる。§6.4の再開判定で発動時期を使う場合は後者を用いること。

## 結論

- **P2（シャドー運用）: 完了**（3サイクル: P2-01週次レビュー / P2-02モデル比較4タスク / P2-03回帰検証）。
- **P3（ランブック改訂）: 完了**（§7行0新設・判定テーブル改訂・用語定義、§6.1手順0・閾値再調整、§6.2範囲明確化、§6.3品質要件、凍結中バグ修正の扱い）。
- 以後は P4（Fable 5 提供終了時の完全移行）待機。それまで週次レビューを通常運用（Opusで実施可）として回し、迷いが出れば §12 のルールで仕様書を改訂し続ける。

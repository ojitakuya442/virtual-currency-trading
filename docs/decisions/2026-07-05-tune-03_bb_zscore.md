# 変更記録: 03_bb_zscore 中立分岐のゲート化（2026-07-05・人間承認済み）

> 1行サマリー: 人間の承認（2026-07-05、ロジック変更＋パラメータ外出しを承認）に基づき、コスト垂れ流しの原因だった中立分岐（ハードコード z<0 & rsi<50）を config パラメータでゲート化。ゲート値は直近8.7日の実データ検証で選定。

背景: 週次レビューで回転過多判定（7日82取引・実現-541円）。シャドー運用P2-02のT2調査で「損失の97.6%が中立分岐の0.2⇄0.0往復、config-onlyでは解消不可」と実証済み（`2026-07-05-model-comparison-p2-02.md`）。

## 変更仮説（1文）

z-scoreゼロ近傍のノイズで発火していた中立ロング（target 0.2）を「明確な下方乖離時のみ」にゲートすれば、本来の逆張りシグナル（z≤-2）を温存したまま往復コストが解消する。

## 変更内容（before → after）

| 項目 | before | after |
|---|---|---|
| `src/bots/bot_03_bb_zscore.py` 中立分岐 | `z < 0 and rsi < 50`（ハードコード） | `z <= p["neutral_entry_z"] and rsi <= p["neutral_entry_rsi"]`（config化） |
| `neutral_entry_z`（新設） | （実質 0） | **-1.5** |
| `neutral_entry_rsi`（新設） | （実質 50） | **35** |

## ゲート値の選定根拠（直近8.7日・15分サンプリングのターゲット遷移数）

| ルール | BTC+ETH計/8.7日 | 実約定換算/7日* |
|---|---|---|
| 旧 (z<0, rsi<50) | 368 | ≈82（実績と整合） |
| z≤-1.0, rsi≤40（当初案） | 225 | ≈50 — **基準(<40)超過のため不採用** |
| z≤-1.25, rsi≤40 | 223 | ≈50 — 同上 |
| **z≤-1.5, rsi≤35（採用）** | **156** | **≈35** |

*実約定換算 = シミュ遷移数 ×（実績82 ÷ 旧ルールシミュ296）。Actionsの実行間隔の揺らぎを吸収するための実績ベース補正。

## 検証（2026-07-05 実施)

- 新パラメータで `compute_signal` が正常動作（現時点はADX>30でトレンド停止中のため target 0）。
- E2E はサンドボックスから Kraken API 到達不可のため未実施。次回 Actions 実行で確認。

## 評価予定日と成功基準

- **評価予定日: 2026-07-12（週次レビュー）**
- 成功基準:
  1. 7日取引数 **<40**（§7行4の回転過多閾値未満）— 必達
  2. 7日実現損益 **≥0** — 必達
  3. 逆張りシグナル（z≤-2）の発火が阻害されていないこと（reason「下方行き過ぎ」の出現をtradesで確認）— 補助
- ロールバック: 取引数≥40 または実現損益が-541円より悪化 → ゲート値を元の実質値(0/50)に戻すのではなく、まず -1.5/35 のまま**中立分岐の完全無効化**（z≤-999 相当）を人間に提案する（分岐自体が負の期待値の疑いが強いため）。

# 修正記録: 10_deriv 複合欠陥の解消（2026-07-05・人間承認済み）

> 1行サマリー: 人間の承認（2026-07-05、閾値修正も含め全部承認）に基づき、10_deriv の3欠陥（保存順序バグ / OI欠損の握り潰し / funding閾値の桁ズレ）を修正。エスカレーション凍結中だが「凍結中でも人間承認があれば実施可」（§7行0）に従い実施。

背景・調査の詳細: `2026-07-05-incident-10deriv.md`

## 変更内容（before → after）

| # | ファイル | 変更 |
|---|---|---|
| C | `src/bots/bot_10_deriv.py` | `get_latest_derivative()`（前回値の読み取り）を `save_derivative_data()` の**前**に移動。旧実装は保存直後の自レコードと比較して `oi_change` が恒等的に0だった |
| B | `src/data_collector.py` `fetch_open_interest` | `openInterestAmount or openInterestValue or 0` の握り潰しを廃止。`info.openInterest` もフォールバック探索し、有効値が無い場合は**警告ログ＋None**（取得失敗扱い）に変更 |
| A | `src/config.py` | `funding_extreme_pct`: **0.01 → 3e-5**。Kraken Futures の実FR分布（74日実測: BTC ±3.3e-5、ETH -6.1e-5〜+3.3e-5）に合わせた。旧値はBinanceスケールの名残（docstringも Binance→Kraken に訂正） |

## 検証（2026-07-05 実施）

- ネットワーク・DB書き込みをスタブ化した5シナリオ検証: FR過熱→クローズ / ショート過熱→0.6 / ショート過熱+OI急増→0.8 / OI急減→0.0 / 中立→0.2 の**全シナリオ期待通り**。
- 10bot全体の import 健全性OK。
- E2E（`run_bots.py` 完走）はサンドボックスから Kraken API に到達できず未実施。**次回の GitHub Actions 実行が実質的なE2E確認**となる（§6.1 の翌日ヘルスチェックで確認すること）。

## 評価予定日と成功基準

- **評価予定日: 2026-07-12（週次レビュー）**
- 成功基準:
  1. `derivatives.open_interest` に**非ゼロ値が記録され始める**、または OI 警告ログが Actions に出る（どちらでも欠陥Bの真因が確定する）— 必達
  2. 10_deriv のシグナル reason に「デリバ中立」以外（FR過熱/ショート過熱/OI急増/OI急減）が**期間中1回以上出現** — 期待
  3. 新規取引が発生する（行5「機能不全」からの脱出）— 期待
- ロールバック: 期待に反して過剰取引（7日40回以上）となった場合は `funding_extreme_pct` を 5e-5 へ引き上げ（§6.3 の通常チューニングとして）。

## 注意（次回レビュー向け）

- OI が引き続き警告になる場合、Kraken Futures が OI を提供していない可能性がある。その場合 OI 系の分岐は恒久的に不発となるため、「FR専業bot」への設計変更を人間に提案すること。
